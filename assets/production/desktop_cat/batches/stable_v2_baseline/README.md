# stable_v2_baseline

This batch records the current accepted full-frame sprite baseline.

Source frames remain in `assets/sprites` so the runnable app and stable preview
continue to work without duplication. This batch exists to make the baseline
explicit for future comparisons.

QA:

- `assets/qa/stable/stable_sprite_contact_sheet.png`
- `assets/qa/stable/*.gif`
